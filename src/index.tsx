import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import Embed from './Embed';

ReactDOM.render(
  <React.StrictMode>
    <Embed />
  </React.StrictMode>,
  document.getElementById('chec-embed')
);
